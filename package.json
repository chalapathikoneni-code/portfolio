{
  "name": "chalapathi-koneni-public-site",
  "private": true,
  "version": "3.0.0",
  "scripts": {
    "build": "node build.js",
    "serve": "node build.js && python3 -m http.server 8000 --directory _site",
    "check": "node scripts/check-links.js"
  }
}
