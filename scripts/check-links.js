const fs=require('fs'),path=require('path');const root=path.join(__dirname,'..','_site');let files=[];function walk(d){for(const f of fs.readdirSync(d)){const p=path.join(d,f);if(fs.statSync(p).isDirectory())walk(p);else if(f.endsWith('.html'))files.push(p)}}walk(root);let bad=[];for(const f of files){const s=fs.readFileSync(f,'utf8');for(const m of s.matchAll(/(?:href|src)="([^"]+)"/g)){let u=m[1];if(/^(https?:|mailto:|#)/.test(u))continue;u=u.split('#')[0].split('?')[0];let p=u.startsWith('/')?path.join(root,u):path.resolve(path.dirname(f),u);if(u.endsWith('/'))p=path.join(p,'index.html');if(!fs.existsSync(p))bad.push(`${path.relative(root,f)} -> ${u}`)}}if(bad.length){console.error(bad.join('\n'));process.exit(1)}console.log(`Checked ${files.length} HTML pages: no broken internal links.`);